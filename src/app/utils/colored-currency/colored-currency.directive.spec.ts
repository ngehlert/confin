import { ColoredCurrencyDirective } from './colored-currency.directive';

describe('ColoredCurrencyDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoredCurrencyDirective();
    expect(directive).toBeTruthy();
  });
});
